# Server
PORT=3001

# More env vars land here in the next pass (DATABASE_URL, ARC_RPC_URL,
# CIRCLE_API_KEY, ANTHROPIC_API_KEY) once the deep backend corrections
# (Postgres persistence, real permission enforcement) are ported over.
